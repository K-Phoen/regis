Regis\GithubContext\Domain\Entity\Report:
    type: entity
    table: report

    id:
        id:
            type: guid
            generator: { strategy: UUID }

    fields:
        warningsCount:
            type: integer
        errorsCount:
            type: integer

    oneToMany:
        analyses:
          targetEntity: Analysis
          mappedBy: report
          cascade: [ 'persist', 'merge' ]
